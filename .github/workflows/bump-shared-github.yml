name: Bump shared-github pin

# TODO: replace <shared-github-sha> below with the actual commit SHA once
# reusable-bump-shared-pin.yml is committed/pushed to fortify/shared-github.

on:
  schedule:
    - cron: '0 6 * * *'   # daily
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  bump:
    uses: fortify/shared-github/.github/workflows/reusable-bump-shared-pin.yml@6b5f260306c9b9d6aadd2a606801c6ffe77cdb14
