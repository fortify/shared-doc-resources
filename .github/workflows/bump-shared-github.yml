name: Bump shared-github pin

# TODO: replace <shared-github-sha> below with the actual commit SHA once
# reusable-bump-shared-pin.yml is committed/pushed to fortify/shared-github.

on:
  schedule:
    - cron: '0 6 * * *'   # daily
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bump:
    uses: fortify/shared-github/.github/workflows/reusable-bump-shared-pin.yml@d9552c48d8270d8e320ff2ab4d356c8a69356686
